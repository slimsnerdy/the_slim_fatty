from .views import SignUpView
from django.urls import path

# from .views import MyView

urlpatterns = [
    # path('signup/', SignUpView.as_view(), name = 'signup'), #comment out until login system launched (for RATINGS/GOATDOM)
    # path('', MyView.as_view(), name='home'),
]